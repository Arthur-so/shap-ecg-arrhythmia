# Dependências do projeto (TCC — Avaliação das explicações SHAP em ECG)
# Versões mínimas testadas; no Kaggle o torch já vem pré-instalado com CUDA.
torch>=2.0
numpy>=1.24
scipy>=1.10
wfdb>=4.1
scikit-learn>=1.3
captum>=0.7
quantus>=0.5
pandas>=2.0
tqdm>=4.65
